import { memo } from 'react';

interface AboutProps {
}

const About = memo((props: AboutProps) => {
const {} = props;
    return (
        <h1>
             About
        </h1>
    );
});

export default About;

