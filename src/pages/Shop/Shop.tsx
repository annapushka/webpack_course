import { memo } from 'react';

interface ShopProps {
}

const Shop = memo((props: ShopProps) => {
const {} = props;
    return (
        <h1>
             Shop
        </h1>
    );
});

export default Shop;

